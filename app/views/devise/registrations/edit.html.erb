<div class="devise-container image-devise">
  <div class="devise-content-container">
    <h2>Atualizar o seu cadastro </h2>

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :email, required: true,  hint: "Para atualizar o seu email corrija o endereço acima.", autofocus: true %>
        <%= f.input :name, label: 'Nome', required: false, hint: "Insira seu nome completo no campo acima.", autofocus: true %>
        <%= f.input :cpf, required: false,  hint: "Insira seu cpf no campo acima.", autofocus: true %>
        <%= f.input :address, label: 'Endereço', required: false,  hint: "Insira seu endereço residencial no campo acima.", autofocus: true %>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
        <% end %>

        <%= f.input :password, label: 'Senha',autocomplete: "off", hint: "Deixe em branco se não quiser alterar", required: false %>
        <%= f.input :password_confirmation, label: 'Confirme sua nova senha',hint: "Deixe em branco se não quiser alterar", required: false %>
        <%= f.input :current_password, label: 'Senha atual',hint: "Para confirmar as mudanças insira a sua senha.", required: true %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Atualizar" %>
      </div>
    <% end %>

    <h3>Cancelar sua conta?</h3>
    <p>Não está satisfeito? <%= link_to "Cancelar minha conta", registration_path(resource_name), data: { confirm: "Tem certeza?" }, method: :delete %></p>
    <%= link_to "Voltar", :back %>
  </div>
</div>
